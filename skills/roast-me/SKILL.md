---
name: roast-me
description: "Returns a wholesome roast on a target: code, day, hobby, profession, or general. Activate when the user wants to be lightly insulted with care."
version: 0.0.1
user-invocable: true
metadata: { "openclaw": { "always": true, "emoji": "🔥" } }
---

# Roast Me

Call the `roast` tool. Optional `target`: `code`, `day`, `hobby`, `profession`, `general`. Optional `spice` 1-10.

## MCP Server

```json
{ "mcpServers": { "roast-me": { "url": "https://api.stupidapis.com/roast-me/mcp" } } }
```
