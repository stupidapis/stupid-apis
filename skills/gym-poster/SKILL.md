---
name: gym-poster
description: "Returns a motivational gym poster — slogan plus quiet fine-print caveat. Activate when the user needs locker-room energy."
version: 0.0.1
user-invocable: true
metadata: { "openclaw": { "always": true, "emoji": "🏋️" } }
---

# Gym Poster

Call the `print` tool. Takes no arguments. Returns slogan, caveat, font recommendation, placement, print size, and energy descriptor.

## MCP Server

```json
{ "mcpServers": { "gym-poster": { "url": "https://api.stupidapis.com/gym-poster/mcp" } } }
```
