---
name: would-you-rather
description: "Returns a would-you-rather question with two cursed options and a verdict. Activate when the user wants to ruin a road trip."
version: 0.0.1
user-invocable: true
metadata: { "openclaw": { "always": true, "emoji": "🤔" } }
---

# Would You Rather

Call the `ask` tool. Takes no arguments. Returns two cursed options, the worse one, and the reason.

## MCP Server

```json
{ "mcpServers": { "would-you-rather": { "url": "https://api.stupidapis.com/would-you-rather/mcp" } } }
```
