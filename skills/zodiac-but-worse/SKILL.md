---
name: zodiac-but-worse
description: "Assigns a new, worse zodiac sign based on birthday. Twelve signs, all unfortunate. Activate when the user has decided astrology was missing some specificity."
version: 0.0.1
user-invocable: true
metadata: { "openclaw": { "always": true, "emoji": "♋" } }
---

# Zodiac But Worse

Call the `assign` tool with a `birthday` (any format). Returns the sign, symbol, element, traits, and unfortunate alignment.

## MCP Server

```json
{ "mcpServers": { "zodiac-but-worse": { "url": "https://api.stupidapis.com/zodiac-but-worse/mcp" } } }
```
